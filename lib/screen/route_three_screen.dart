import 'package:flutter/material.dart';

class RooueThreeScreen extends StatelessWidget {
  const RooueThreeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Route Three"),
      ),
    );
  }
}
