import 'package:flutter/material.dart';

class RooueTwoScreen extends StatelessWidget {
  const RooueTwoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Route Two"),
      ),
    );
  }
}
